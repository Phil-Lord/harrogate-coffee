// The site's one horizontal rhythm, so full-width chrome lines up with the
// cards. Bar heights are its counterpart in `globals.css`.
export const pageContainer = 'mr-auto w-full max-w-5xl px-6 sm:px-18'
